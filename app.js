const root = document.getElementById('root');const routes = {'/': 'Home','/fleet': 'Fleet Details'};function renderRoute(route){const urlParts = route.split('/');const lastPart = urlParts[urlParts.length - 1];switch(lastPart){case '':return '<h1>Home Page</h1>';case 'fleet':return '<h1>Fleet Details Page</h1>';default:return '<h1>404: Page Not Found</h1>';}}function navigate(url){history.pushState({}, '', url);render(root, url);}function render(container, url){container.innerHTML = renderRoute(url);}window.addEventListener('popstate', (event) => {render(root, window.location.hash.slice(1));});navigate(window.location.hash.slice(1) || '/');